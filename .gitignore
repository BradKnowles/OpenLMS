## This .gitignore is non-standard, it is an allow list.
## Files must be explicitly added as exclusions to be committed.

# Deny files/folders in same directory as the .gitignore file
/*

# Deny all files
*.*

# Deny explicitly even if contains allowed files
bin/
obj/

# Allow git configuration files
!.gitignore
!.gitattributes

# Allow subdirectories so the rest of the rules work
!src
!tests

# Allow source files
!*.sln
!*.csproj
!*.cs
!appSettings*.json

# Allow configuration files
!global.json
!.editorconfig

# Allow MSBUILD files
!*.props
!*.targets

# Allow documentation files
!*.md
